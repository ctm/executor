/* Copyright 1995 by Abacus Research and
 * Development, Inc.  All rights reserved.
 */

#if !defined (OMIT_RCSID_STRINGS)
char ROMlib_rcsid_finder[] =
	    "$Id: finder.c 63 2004-12-24 18:19:43Z ctm $";
#endif

#include "rsys/common.h"

#include "Finder.h"
#include "FileMgr.h"

#include "rsys/file.h"

PUBLIC OSErr
PBDTGetPath (DTPBPtr dtp)
{
  OSErr retval;

  retval = extFSErr;
  return retval;
}

PUBLIC OSErr
PBDTOpenInform (DTPBPtr dtp)
{
  OSErr retval;

  retval = extFSErr;
  return retval;
}

PUBLIC OSErr
PBDTCloseDown (DTPBPtr dtp)
{
  OSErr retval;

  retval = extFSErr;
  return retval;
}

PUBLIC OSErr
PBDTGetIcon (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTGetIconInfo (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTGetAPPL (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTGetComment (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTAddIcon (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTAddAPPL (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTSetComment (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTRemoveAPPL (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTRemoveComment (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTFlush (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTGetInfo (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTReset (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}

PUBLIC OSErr
PBDTDelete (DTPBPtr dtp, BOOLEAN async)
{
  OSErr retval;

  retval = extFSErr;
  FAKEASYNC (dtp, async, retval);
  return retval;
}
