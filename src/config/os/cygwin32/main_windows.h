#if !defined (__main_windows_h__)
#define __main_windows_h__

/*
 * This prevents warning messages from being passed on to the user, so
 * eventually we probably don't want to do this
 */

#define WarningAlert(msg)

#endif
